pipeline {
  agent any
  stages {
    stage("build") {
      steps {
        echo "builidng the application with commit pushes"
      }
    }
    
    stage("test") {
      steps {
        echo "testing the application"
      }
    }
  }
}
